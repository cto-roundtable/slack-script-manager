{
  "name": "slack-member-comparer",
  "version": "1.0.0",
  "description": "Compare members across Slack channels",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["slack", "cli", "typescript", "bun"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@slack/web-api": "^7.6.0",
    "commander": "^12.0.0",
    "console-table-printer": "^2.12.1",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
